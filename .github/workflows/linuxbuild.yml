name: LinuxBuild

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: prepare
      run: ./linux/prepare.sh
      shell: bash
    - name: compile
      run: ./linux/compile.sh
      shell: bash
